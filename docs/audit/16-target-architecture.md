# Target Architecture
Move towards a strict separation of concerns:
1. **Data Layer**: Prisma `WeddingData`.
2. **Engine Layer**: Shared Headless Components (Sliders, Modals).
3. **Presentation Layer**: Template-specific design tokens and CSS.